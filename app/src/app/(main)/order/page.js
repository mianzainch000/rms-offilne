import OrderEditorTemplate from "./template";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Order",
};

export default function OrderPage() {
  return <OrderEditorTemplate />;
}
